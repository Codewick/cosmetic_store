<%= form_for([@product,@product.reviews.build]) do |form|  %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>
      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
<% if !current_user.admin? %>
  <div class="field">
     <%= form.label :content %>
    <br>
    <%= form.text_area :content,
      :placeholder => "Write your reviews...", id: :review_content %>
  </div>

  <div class="form-group row">
     <!--<%= form.label :"Enter rating (1-5):", class: 'col-sm-2 form-control-label' %> -->
     <h3> Enter Rating (1-5)</h3>
     <div class="col-sm-6">
       <%= form.number_field :rating %>
     </div>
 </div>

  <!-- <div class="field">
    <%= form.label :user_id %>
    <%= form.hidden_field :user_id, :value => current_user.id, id: :review_user_id%>
  </div> -->
<br>
  <div class="actions">
    <%= form.submit :class=> "btn btn-round mt-4 mb-5 mb-md-0"%>
  </div>
  <% end %>
<% end %>
